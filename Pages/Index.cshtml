@page
@using PersonalSite.Services
@model PersonalSite.Pages.IndexModel
@inject DiscordService DiscordService

@{
  var user = await DiscordService.GetUserAsync();
}

<main>
  <h1>Profile</h1>
  
</main>